
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/_LayoutLogin.cshtml";
}

<!doctype html>
<html lang="en">
<head>
    <title>Login 08</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
    <script src="https://code.jquery.com/ui/1.13.1/jquery-ui.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Lato:300,400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="~/LoginAssets/css/style.css">
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
</head>
<body>
    <section class="ftco-section">
        <div class="container">

            <div class="row justify-content-center">
                <div class="col-md-6 col-lg-5">
                    <div class="login-wrap p-4 p-md-5">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="fa fa-user-o"></span>
                        </div>
                        @*@Url.Action("Index", "Home")*@
                        <h3 class="text-center mb-4">WELCOME</h3>
                        <form method="post" class="" id="loginForm">
                            <div class="form-group">
                                <input type="text" name="Name" id="txtUserName" class="form-control rounded-left" placeholder="Username" required>
                            </div>
                            <div class="form-group d-flex">
                                <input type="password" name="Password" id="txtPassword" class="form-control rounded-left" placeholder="Password" required>
                            </div>
                            <div class="form-group d-md-flex">
                                <div class="w-50">
                                    <label class="checkbox-wrap checkbox-primary">
                                        Remember Me
                                        <input type="checkbox" checked>
                                        <span class="checkmark"></span>
                                    </label>
                                </div>
                                <div class="w-50 text-md-right">
                                    <a href="#">Forgot Password</a>
                                </div>
                            </div>
                            <div class="form-group">

                                <button type="button" id="btnsub" class="btn btn-primary rounded submit p-3 px-5">lOG IN</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
<div class="modal fade" id="idMsg" tabindex="-1" role="dialog" data-backdrop="Static">
    <div class="modal-dialog modal-dialog-centered modal-sm" role="document">
        <div class="modal-content">
           
            <div class="modal-body">
                <div id="msg">
                    Wrong UserName or Password !!!!
                </div>
                
            </div>
        </div>
    </div>
</div>
    <script src="~/LoginAssets/js/jquery.min.js"></script>
    <script src="~/LoginAssets/js/popper.js"></script>
    <script src="~/LoginAssets/js/bootstrap.min.js"></script>
    <script src="~/LoginAssets/js/main.js"></script>
    <script type="text/javascript">
        $('#btnsub').click(function () {
            var Obj = {}
            Obj.Name = $('#txtUserName').val()
            Obj.Password = $('#txtPassword').val()
            $.ajax({
                url: '@Url.Action("Verify", "Login")',
                type: 'POST',
                dataType: 'JSON',
                data: { login: Obj },
                success: function (data) {
                    if (data == 0) {
                        window.location.replace('@Url.Action("Dashboard","Dashboard")')
                    } else if (data == 1) {
                        $('#idMsg').modal('show')
                    }
                }
            });
        });
       //var form = document.getElementsByClassName('loginForm');
       // var Validation = Array.prototype.filter.call(form, function (forms) {
       //     forms.addEventListener('submit', function (event) {
       //         if (forms.checkValidity() === false) {
       //             event.prevenDefault();
       //         } else {
       //             event.prevenDefault();
       //             alert('fgsdgs')
       //         }
            @*$.ajax({
                url: '@Url.Action("Verify","Login")',
                type: 'POST',
                dataType: 'JSON',
                data: { username: $('#txtUserName').val(), password: $('#txtPassword').val() },
                success: function (data) {
                    if (data.errCode == 0) {
                        window.location.replace('@Url.Action("Index","Home")')
                    } else {

                    }
                }
                }
            })*@
                    @*$.ajax({
                        url: '@Url.Action("Verify","Login")',
                        type: 'POST',
                        dataType: 'JSON',
                        data: { username: $('#txtUser').val(), password: $('#txtPassword').val() },
                        success: function (data) {
                            if (data == "Fail") {
                                 $("#loginForm")[0].reset();

                             }
                            else {
                                window.location.href = "@Url.Action("Index","Home")";

                             }
                             })*@


                    @*$("#btnsub").click(function () {

                        document.location = '@Url.Content("~/Login/Verify?username=")' + $('#txtUserName').val() + ' &password=' + $('#txtPassword').val();
                    });*@
         @*//   alert('fgsdgs')
         //   $.ajax({
         //       url: '@Url.Action("Verify","Login")',
         //       type: 'GET',
         //       dataType: 'JSON',
         //       data: { username: $('#txtUserName').val(), password: $('#txtPassword').val() },
         //       success: function (data) {
         //           if(data.errCode == 0){
         //window.location.replace ('@Url.Action("Index","Home")')*@
         //}else{

              //   }

              //       }
              //   })


        //function ClickLogin() {
            @*alert("hh")
            $.ajax({
                url: '@Url.Action("Verify","Login")',
                type: 'POST',
                dataType: 'JSON',
                data: { username: $('#txtUser').val(), password: $('#txtPassword').val() },
                success: function (data) {
                    if (data == "Fail") {
                        $("#loginForm")[0].reset();

                    }
                    else {
                        window.location.href = "@Url.Action("Index","Home")";

                    }
                }
            })
        }*@
    </script>
</body>
</html>

